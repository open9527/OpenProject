android {
    //支持dataBinding
    dataBinding {
        enabled true
    }
}

dependencies {
//    api project(gradle.getUnionPath(":feature:video:export"))
//    kapt project(gradle.getUnionPath(":apt:compiler"))
}