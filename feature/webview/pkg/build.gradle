
android {
    //支持dataBinding
    dataBinding {
        enabled true
    }
}

dependencies {
    api  project(gradle.getUnionPath(":feature:webview:export"))
    kapt project(gradle.getUnionPath(":apt:compiler"))

}