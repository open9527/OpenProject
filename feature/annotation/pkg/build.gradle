
android {
    //支持dataBinding
    dataBinding {
        enabled true
    }
}

dependencies {
    api project(gradle.getUnionPath(":feature:annotation:export"))
    kapt project(gradle.getUnionPath(":apt:compiler"))
}